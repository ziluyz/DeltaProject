#include "main.h"
#include <cmath>

int maincalc() {
    using namespace std;
    /*for (auto a : input::va) output::va.push_back(a);
    for (auto b : input::vb) output::vb.push_back(b);
    output::va.setValid();
    output::vb.setValid();*/
    return 0;
}
